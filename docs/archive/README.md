# Archive — 历史文档(已删除/废弃模块)

这里的文档描述的系统**已不存在或已废弃**,保留作历史记录。

> ⚠️ 不要照这里的文档去理解当前代码——它们引用的模块(如 judge、driver-session)已在 `ddd5520`(2026-06-27)整体删除。当前实现见 `docs/design/` 和 `docs/handoff/` 下的最新文档。

归档原因:
- **Judge 模块**(2026-06-19 至 2026-06-22 的 handoff/design):judge agent + driver-session.ts 机制已整体删除,这些文档描述的系统不再存在。
- 保留是为了追溯当时的设计决策和故障记录,不作为当前实现依据。

如需了解当前 /task、CDP、MCP 等系统的设计,看 `docs/design/` 下对应主题的最新文档。
