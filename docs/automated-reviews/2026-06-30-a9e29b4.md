# Automated Review - 2026-06-30 `a9e29b4`

- Review type: diff review
- Scope: `cf54a8f55806b0ba146cc40eca85e80f4de4b658..a9e29b45dbc9ee044118f4ebe17a37aa69140ff0`
- Base commit: `cf54a8f55806b0ba146cc40eca85e80f4de4b658`
- Head commit: `a9e29b45dbc9ee044118f4ebe17a37aa69140ff0`
- Changed implementation modules: `cron/*`, `extensions/doctor/*`, `extensions/device-env.ts`, `extensions/index.ts`, `extensions/subagent-*`, `extensions/task/*`, `skills/ugk-environment-doctor/*`, bundled skill/docs cleanup, related tests.
- Context note: repo 中没有 `CONTEXT.md` 或 `docs/adr/`，以下建议使用现有代码 Module 名称。
- Verification note: 本次自动化只做审核和报告；未修改业务代码、未安装依赖、未跑完整测试套件。

## Ponytail audit findings

- `delete:` `/doctor` 已迁移为引导式 `ugk-environment-doctor` skill，但旧一次性健康检查 Implementation 仍留在运行时代码里。Replacement: 删除 `extensions/doctor/checks.ts`、`extensions/doctor/formatter.ts` 以及只覆盖死路径的 `tests/doctor-checks.test.ts`、`tests/doctor-formatter.test.ts`；保留 `/doctor` migration notice 测试。 [`extensions/doctor/*`, `skills/ugk-environment-doctor/*`, `tests/doctor-*.test.ts`]
- `shrink:` `extensions/doctor/types.ts` 现在主要被 `createMcpDoctorCheck()` 借用一个 `DoctorCheck` 形状。Replacement: 若删除旧 doctor checks，把最小 `DoctorCheck`/`DoctorResult` 类型移到 MCP 自己的 test seam 或 `extensions/mcp` 内；不要为了一个旧 `/doctor` 表格保留整包 doctor 类型。 [`extensions/doctor/types.ts`, `extensions/mcp/index.ts`, `tests/integration/mcp-extension.test.ts`]
- `yagni:` `DoctorDeps.checks` 已被 `registerDoctor()` 显式 `void deps` 忽略，测试仍传 fake checks 来证明不会执行。Replacement: 删除该参数和 fake checks；直接测试 notice 文案即可。 [`extensions/doctor/index.ts`, `tests/doctor-extension.test.ts`]
- Lean already: Android 默认工具和 adb/scrcpy skills 被删除，`device-env.ts` 缩回 `getUgkBin()`，这是正确的删除方向；不建议补兼容层。
- Lean already: bundled install agents 通过 `discoverAgents()` 的现有 Map 优先级接入，没有新增 registry/factory；这块不用再抽。
- Lean already: task 进度只推 assistant summary 首行和失败 toolResult，成功 toolResult/ToolCall 被跳过，当前 Interface 足够窄。

net: -500 to -540 lines, -0 deps possible.

## Architecture candidates

### Candidate 1 - Strong

- Files: `extensions/doctor/index.ts`, `extensions/doctor/checks.ts`, `extensions/doctor/formatter.ts`, `extensions/doctor/types.ts`, `skills/ugk-environment-doctor/*`, `tests/doctor-*.test.ts`, `extensions/mcp/index.ts`
- Module: environment doctor
- Problem: environment doctor Module is shallow after the migration: `/doctor` Interface now only shows a notice, while the old check Implementation and formatter remain as a second, unused Interface that tests keep alive.
- Solution: deepen around the guided skill Interface. Keep `registerDoctor()` as a tiny legacy entrypoint, keep the skill/script docs as the active Implementation, and delete the unused one-shot check/formatter path. Move any still-needed MCP doctor type locally.
- Benefits: Locality: environment repair behaviour lives in one skill Module; Leverage: tests cover the Interface users still reach; Depth improves because stale checks stop leaking maintenance into runtime code.

### Candidate 2 - Worth exploring

- Files: `cron/agent-bin.ts`, `cron/service.ts`, `tests/cron-agent-bin.test.ts`
- Module: cron agent invocation
- Problem: `getCronAgentBin()` returns a shell-ready string that sometimes contains both executable and path arguments, so `service.ts` must know to set `shell:true`. The Interface leaks command quoting details from the Implementation.
- Solution: Return `{ command, argsPrefix, shell }` or reuse the existing subagent invocation shape, then let `service.ts` append `--print`, prompt, and model as data. Keep the fallback to bundled `bin/ugk.js`; just stop encoding it as one shell string.
- Benefits: Locality: quoting policy sits in one Module; Leverage: cron tests can assert invocation shape without real shell parsing; Interface shrinks for callers.

### Candidate 3 - Speculative

- Files: `extensions/task/task-worker.ts`, `extensions/task/task.ts`, `tests/task-worker.test.ts`
- Module: task worker progress
- Problem: progress extraction currently scans all worker messages on each partial update and relies on downstream de-dupe. This is intentionally simple and bounded by worker turn count.
- Solution: Keep it. Only add cursor/state to the progress Interface if large worker transcripts make the O(n) scan visible in real UI latency.
- Benefits: Locality is already acceptable; adding state now would widen the Interface for a speculative performance issue.

## Top recommendation

Do Candidate 1 first. It is a direct deletion win created by this diff: the guided environment doctor skill replaced the old one-shot doctor table, so keeping both paths reduces locality and makes tests protect code users no longer reach.

## Skipped and why

- Full-repo audit skipped: `lastFullReviewDate` is `2026-06-27`, `fullReviewPeriodDays` is `7`, and this run is still inside the diff-review window.
- Previous automated report commit was included in the diff but treated as generated audit history, not a runtime Module: `docs/automated-reviews/2026-06-29-cf54a8f.md`.
- Large handoff/design docs were used only as context; they were not re-audited as implementation unless they pointed at changed Modules.
- Correctness, security, and performance bugs were out of scope for `ponytail-audit`; the cron shell invocation concern is framed as an Interface/depth candidate, not as a bug fix.
- No code fixes were applied because this automation is audit/report-only.
