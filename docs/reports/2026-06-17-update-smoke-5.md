# UGK update smoke marker 5

This marker intentionally advances `origin/main` so a local UGK build that already
contains the Windows `cmd.exe /c npm install` fix can exercise the managed update
flow end to end.
